<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base"
        parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme"
        parent="AppTheme.Base"/>

    <style name="AppSplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_window</item>
    </style>

    <!-- Toolbar 主题 -->
    <style name="AppTheme.ToolbarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/WHITE</item>  <!-- 溢出按钮颜色-->
        <item name="actionMenuTextColor">@color/WHITE</item> <!-- 溢出文字颜色 -->
    </style>

    <!-- Toolbar 标题样式 -->
    <style name="ToolbarTitle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/WHITE</item>
    </style>

</resources>
